# Fixed: contenidor amb capabilities minimes necessaries
FROM ubuntu:22.04

RUN apt-get update && apt-get install -y python3

RUN useradd -m -u 1001 appuser

WORKDIR /app
RUN chown -R appuser:appuser /app

USER appuser

CMD ["python3", "-m", "http.server", "8080"]
